export type PaperSource={title:string;url:string};
export function sourcePaperPdf(sources:PaperSource[]){for(const source of sources){try{const url=new URL(source.url);if(url.protocol!=='https:')continue;if(/\.pdf(?:$|[?#])/i.test(url.pathname+url.search+url.hash))return url.href;const arxiv=url.hostname==='arxiv.org'&&url.pathname.match(/^\/(?:abs|html)\/([^/]+)$/);if(arxiv)return 'https://arxiv.org/pdf/'+arxiv[1].replace(/v\d+$/i,'');}catch{}}return '';}
export type PaperResourceKind='code'|'dataset'|'model'|'project'|'supplement';
export type PaperResource=PaperSource&{kind:PaperResourceKind};
export function paperResources(sources:PaperSource[]){const seen=new Set<string>(),result:PaperResource[]=[];for(const source of sources){try{const url=new URL(source.url);if(url.protocol!=='https:'||seen.has(url.href))continue;const host=url.hostname.replace(/^www\./,''),text=(source.title+' '+url.pathname).toLowerCase();let kind:PaperResourceKind|undefined;if(/github\.com|gitlab\.com|bitbucket\.org/.test(host)||/\b(code|repository|repo|implementation)\b/.test(text))kind='code';else if(/kaggle\.com|zenodo\.org|figshare\.com|dryad\.org|dataverse|data\.mendeley\.com/.test(host)||/\b(dataset|data set|benchmark data|corpus)\b/.test(text))kind='dataset';else if(host==='huggingface.co'&&/\/(?:models?|spaces?)\b/.test(url.pathname)||/\b(model weights?|checkpoint|demo)\b/.test(text))kind='model';else if(/\b(supplement|supplementary|appendix|artifact)\b/.test(text))kind='supplement';else if(/paperswithcode\.com|osf\.io/.test(host)||/\b(project page|project site|website)\b/.test(text))kind='project';if(kind){seen.add(url.href);result.push({...source,url:url.href,kind});}}catch{}}return result;}
