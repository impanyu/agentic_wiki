import {getActor} from '@/app/actor';
import {getPage,reply,sameOrigin} from '@/db/store';
import {connections,callConnector} from '@/app/connectors/service';
import {z} from 'zod';
const reads=new Set(['list_files','list_folders','folder_info','file_metadata','read_text_file']);
export async function GET(request:Request,{params}:{params:Promise<{id:string}>}){try{const actor=await getActor(request);if(!await getPage((await params).id,actor.userId))return reply({error:'PAGE_UNAVAILABLE'},404);const accounts=(await connections(actor.userId)).filter(c=>c.provider==='adma'&&c.enabled&&c.connected).map(c=>({id:c.id,name:c.name,allowed:c.allowed}));return actor.finish(reply({accounts}));}catch{return reply({error:'Could not load ADMA connections.'},400);}}
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{if(!sameOrigin(request))return reply({error:'Invalid origin.'},403);const actor=await getActor(request),pageId=(await params).id;const raw=z.object({connectorId:z.string().uuid(),tool:z.enum(['list_files','list_folders','folder_info','file_metadata','read_text_file','create_folder','update_file','update_folder','delete_file','delete_folder','upload_text_file']),args:z.record(z.unknown()),confirm:z.boolean().optional()}).strict().parse(await request.json());const c=(await connections(actor.userId)).find(c=>c.id===raw.connectorId&&c.provider==='adma');if(!c)throw Error('ADMA connection unavailable.');if(!reads.has(raw.tool)&&raw.confirm!==true)throw Error('Confirm this change first.');const result=await callConnector(actor.userId,c.id,raw.tool,raw.args,pageId);return actor.finish(reply({result}));}catch(e){return reply({error:e instanceof Error?e.message:'ADMA request failed.'},400);}}
