import {getActor} from '@/app/actor';import {reply,sameOrigin} from '@/db/store';import {resolveConnectorAction} from '@/app/connectors/service';
export async function POST(r:Request,{params}:{params:Promise<{id:string}>}){const a=await getActor(r);if(!sameOrigin(r))return a.finish(reply({error:'Invalid origin.'},403));try{const d=await r.json() as {approve:unknown};if(typeof d.approve!=='boolean')throw Error('Choose approve or cancel.');return a.finish(reply({result:await resolveConnectorAction(a.userId,(await params).id,d.approve)}));}catch(e){return a.finish(reply({error:e instanceof Error?e.message:'Connector action failed.'},400));}}
