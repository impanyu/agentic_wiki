import {customStyleFormat} from './custom-style';
const programContract="Write a reusable backend VIEW program for this page, in JavaScript or Python. A pre-coded renderer displays the returned view; to give the app its own interface, also supply the optional frontend field (read_generation_contract kind=frontend) instead of putting HTML in the view. JavaScript exports async function main({input,results}); Python defines main(state) with state['input'], state['results']. It runs in an isolated network-disabled Linux VM with standard library, no credentials, environment access or provider SDKs assumed. Authorized context attachments are mounted at paths in input.files [{id,name,path?,status?}]; read those files when relevant, and handle missing paths honestly. Encode the procedure as deterministic code. Declare inputFields as an array of {name,type:string|number|boolean,description,required}. This is the public input contract used by the routing agent to extract arguments from future questions. Read those arguments from input.values; input.query holds the original navigation question. If required arguments are missing, return a form or clarification view instead of fabricating inputs. Never treat input.query as authorization for a write action. Each run restarts: check results for a step ID before requesting it. Return {call:{id,tool,args}} for one missing result, {calls:[{id,tool,args},...]} (up to 100, run in parallel) to fetch many results at once, for example every daily file of a month, or {view:{templateId,title,summary,...}} when finished. At most 15 steps then a final view. Step ids may use letters, digits and _ . : -. connectors.call of read_text_file from offset 0 returns the whole file (up to 2 MB), so parse it directly; results can total about 12 MB. Never request a completed step again. Available tools: connectors.list args {} discovers CURRENT visiting user enabled tools and JSON schemas; it returns a plain ARRAY (not wrapped in an object) of the connected, enabled connectors only: [{id,name,tools:[{name,description,inputSchema,confirmationRequired}]}]; a connector that is missing from the array is not connected. Pick a connector by the tool it offers (for example the one with list_files for the requested service) or by a case-insensitive name match, never by exact display name alone. connectors.call args {connectorId,tool,arguments} calls one discovered tool and returns that tool's own JSON result, or {error}. resources.browse returns {items:[resource],next} for a folder, or {roots:[resource],limits:{fileBytes,totalBytes,items}} without one. Read results in exactly these shapes; do not guess wrappers. Discover on EVERY runtime execution; NEVER hardcode a creator-specific connector ID in reusable code. Disabled connectors fail. Calls needing review return confirmationRequired and actionId; direct users to Connectors. Do not claim proposed calls executed. Available tools: resources.browse args {folder?:{space:page|google|adma|dropbox|onedrive|hcc|adapt (hcc: HCC Home, Work and NRDStor on Swan; adapt: the ADAPT project share over the UNL VPN; both need connectorId like adma),id,kind:file|folder,name,connectorId?},cursor?}; omit folder to discover roots. resources.copy args {operationId:UUID,sources:[returned resource objects],destination:folder resource}; resources.move takes the same args plus confirmRemoval:true, copies, verifies every copied file, and only then removes the originals (ADMA deletes, cloud storage trashes); offer Move only behind an explicit confirmation; requires explicit input.message or a submitted form/button (input.submitted=true, e.g. pageTools.run(values,{submitted:true})), never on ordinary navigation; supports 10 MB/file, 100 MB/operation and 200 entries and reports partial completion. Use IDs returned by browsing; preserve the same operationId on retry. The shared page chat @ picker and Page files Browse & copy files already provide a GUI for these operations. A transfer request (copy, move, sync or back up files between places) needs its own transfer workspace in the app: source and destination panes over every root from resources.browse, multi-select, an exact destination folder, a Copy/Move button enabled only when both are chosen, resources.copy only after that submit, and a per-item result report; a single-provider file browser does not fulfil it. Available tools: contexts.search args {page_kind:static|dynamic|all,topic_terms:JSON-encoded string array of synonymous topic terms} searches the current user\u2019s actual saved contexts; jobs.list args {} lists their real running tasks; storage.connections args {}; storage.execute args {provider:google|dropbox|onedrive,operation:list|read|mkdir|upload|rename|move|copy|trash,args:{id?,parent?,name?,cursor?,content?}}; data.list args {after?}; files.list args {} lists authorized current-page uploads; files.read args {fileId,options?:{mode:auto|text|table|pdf|image|archive,sheet?,start?,count?,column?,columns?,query?}} reads bounded text/table ranges or file metadata; files.analyze args {fileId,options?,prompt} uses the multimodal model to understand a selected image, PDF pages or document and returns answer plus file metadata. Use files.read for exact spreadsheet cells and files.analyze for visual/document interpretation; follow next cursors and never assume omitted content was read; api.execute args {componentId,version,operation,parameters} only for real known IDs; research and llm args {prompt}. Calls return JSON or {error}; handle every error. Storage mutations return confirmationRequired,actionId,request and are NOT executed until user approval. Do not claim proposed changes succeeded. Input is {message?,parent?,cursor?,values?,context?}; input.context contains the persistent conversation summary and recent turns. Include it in llm prompts for continuity. Do not embed user-specific credentials or account identifiers into this reusable program. Input is {message?,parent?,cursor?,values?,context?}; ordinary navigation has empty input. Never propose file mutations on ordinary navigation; only on a clearly relevant explicit input.message or submitted form. For natural-language interaction use a fixed llm step; it returns {text}. Do not rerun open-ended planning: control flow is in your code. The view templateId must be the draft templateId. Allowed view fields: map:{title,geojson:{type:FeatureCollection,features:[{type:Feature,geometry:{type:Point|MultiPoint|LineString|MultiLineString|Polygon|MultiPolygon,coordinates:WGS84_coordinates},properties:{name,...}}]}},title,summary,body (Markdown),reply, sources:[{title,url:https}], files:[{id,name,kind:file|folder,parents?:string[],modifiedTime?:string}],next:string|null, form:{kind:form,submit,fields:[{name,label,type:number|text,default:null}],outputs:[{name,label}]},results:{name:scalar}; dashboard/table also chart:{kind:chart,xLabel,unit,series:[{key,label}],labels:{line,bar,data,download}},dataset:{rows:[{x,values:{series_key:number|null},sources:[one_based_source_numbers]}],sources:[{title,url}],notes}. ADMA processing calls run_seeding_tool/run_shape_to_json/run_si_tool/run_yield_summary/run_valid_yield_extractor require input.submitted=true (set by the form submit button) or an explicit input.message. Navigation and Refresh cannot start jobs. Supply a UUID operation_id and reuse it on retries. Return task_id visibly and in a form field so later status checks can use processing_status without repeating submission. Prefer the native adma-tools app for standard runs. The optional map field embeds the existing ArcGIS viewer directly inside any program web app and can coexist with charts, forms and results. Read coordinates and boundaries from authorized connector results at runtime. GeoJSON positions are [longitude,latitude]; never guess coordinates or fabricate boundaries. Include service/file attribution in sources and feature properties; missing locations must be reported, not geocoded without evidence. Use bounded data (up to 10,000 features, 200,000 positions) and concise properties within runtime output limits. Use real tool results only; never invent files, numbers, API IDs or connections. Present setup/error states honestly. For files discover all enabled connectors and select tools for the requested service; ADMA and other API/MCP file services use connectors.call. Only Google Drive, Dropbox and OneDrive use storage connections and storage.execute. Combine multiple connectors when the task needs them; data.list covers uploaded data. Honor input.parent and input.cursor. The pre-coded renderer behaves like this, so the program never needs UI code for it: a Refresh button reruns the program with the same input; files render as a file browser, and when no entry has parents, clicking a folder reruns the program with input.parent set to that entry's id (\u201cAll files\u201d and the breadcrumb root send input.parent='root'), so a folder-at-a-time backend lists input.parent's children and uses each entry's id as the path to open it; entries with parents:[folderId] are also navigated locally when a whole tree is returned; a non-null next shows a Next page button that reruns with input.cursor; a form's submit button reruns with input.values and input.submitted=true. No secrets, URLs with embedded credentials, user-specific outputs or transient data in saved program. Respond in the requested language the question language. All user-visible strings, form labels, select options, button text, statuses and help text must be in this language; preserve only machine keys, URLs and proper names.";
// The complete page artifact format plus the generator's own guidance, so the in-page agent can rebuild a page as anything the generator can create.
// What the wiki article renderer actually displays. Anything else appears as plain text.
export const wikiRenderingContract={supported:'Headings # / ## / ### (each on its own line; ### is the deepest); paragraphs separated by blank lines; bullet lists with "- " or "* " and numbered lists with "1. "; **bold** and *italic*; inline citations [n] — n is the 1-based position in sources — render as a linked superscript to that source (a link [n](https://url) whose text is a number or domain also does); links [text](https://url); pipe tables with a |---| separator row; a fenced ```chart block whose JSON is {"chart":{...},"dataset":{...}} (see kind chart); a video embed as a single-line <iframe src="https://www.youtube.com/embed/..."></iframe>; an image as a standalone line ![caption](https://... or /api/pages/<pageId>/files/<fileId>) optionally followed by a line [credit](https://source) — the URL must serve an actual image; a diagram you draw yourself goes through write_page_file (an .svg) and is embedded the same way.',notSupported:'Other raw HTML, nested lists, images inside sentences, footnote syntax, LaTeX, task lists. Blank lines between blocks matter; do not indent block starts.',structure:'summary is the Overview shown above the body; body is the article; sources:[{title,url}] renders the source list; indexEntries drives a disambiguation index instead of the body.'};
const draftContract={fields:'kind:"article"|"disambiguation"|"chat"|"files"|"index"|"program"|"chart"|"form"|"converter"|"native", title(1-200), summary(1-1200), body: Markdown (<=40000, default ""), category (<=100), sources:[{title,url}] (<=40), labels:{string:string}, intent:{visualTheme,visualDesign?,subject,subjectType,goal,explicitConstraints:[],assumptions:[],uncertainties:[],mustCover:[>=1],sourceRequirements:[],outputKind:"article"|"chart"|"application"|"conversation",service:"none"|"context_pages"|"user_jobs"|"google_drive_folders"}, entries? (disambiguation index entries), templateId?:"files-v1"|"dashboard-v1"|"table-v1"|"form-v1"|"chat-v1"|"geo-v1"|"data-tools-v1"|"paper-v1", nativeApp?:"arcgis-publisher"|"adma-tools"|"unl-hcc"|"map"|"table"|"json"|"text"|"image"|"pdf"|"archive"|"hub", interactive?:{kind:"sandbox-app",html,css,javascript,height} (static illustration), program? (see kind program), frontend? (custom UI for kind program; see kind frontend), inputFields?, chart?, dataset?, form?, expression?, examples?, parameters:{} (default {})',perKind:'Call read_generation_contract again with program, chart, form, converter or custom_style for those payloads.',usage:'For an existing page, submit the complete draft with propose_app_revision {kind:"replacement",draft}; validate first with validate_page_draft, test sandbox programs with test_page_program, and run verify_app until an app passes.'};
// A custom interface for a program app. It runs in an isolated iframe and reaches the page's own
// backend through window.pageTools.run, so the UI and the logic stay separate and both are verified.
export const frontendContract={fields:'frontend:{placement:"replace"|"before"|"after",frontend:{kind:"sandbox-app",html:string(<=30000),css:string(<=16000),javascript:string(<=30000),height:40..1200}} on a kind=program draft (also used by the in-page agent as {kind:"code",code:<same object>}).',bridge:'await window.pageTools.run(values, {query?, submitted?:true, message?}) runs this page\u2019s backend program with input.values=values and resolves {view, runtimeError, proposals}; view is exactly the object your program returned. Use submitted:true only from an explicit user action (a submit/run button). await window.pageTools.call(name,args) reaches workspace tools (list_connectors, call_connector, browse_resources, …) for signed-in owners; prefer putting data access in the backend and calling run. No network, imports, external scripts/fonts, cookies, storage APIs or parent DOM; inline SVG and canvas are fine.',architecture:'Keep logic in the backend program (fetching, parsing, computing, validating) and presentation in the frontend. The backend returns a structured view with the data the UI needs (use body for Markdown, results/dataset/form for structures, or put JSON in reply). The frontend calls run on load (read-only) and on user actions, renders loading, empty and error states, and never fabricates data.',design:'Build a polished, practical product UI, not a demo: a clear title and one-line purpose; primary action obvious; logical grouping in cards or sections with generous whitespace (8px spacing scale), aligned grid, max content width ~1100px; system-ui font, 15-16px body, 1.5 line height, clear heading scale; restrained palette with one accent color and WCAG AA contrast; consistent 8-12px radius and subtle borders or shadows; labelled inputs with sensible defaults and inline validation; buttons with hover/focus/disabled states and a busy indicator while running; tables with sticky headers, right-aligned numbers and number formatting; charts drawn with inline SVG or canvas with axes, labels, units and a legend; skeleton or spinner while loading, friendly empty state, readable error with a retry button; responsive down to 390px (stack columns, no horizontal scroll, touch targets >=40px); keyboard accessible with visible focus and aria-labels on icon buttons. Set height to fit the content at 1280px (max 1200). Avoid lorem ipsum, fake numbers, emoji clutter, giant hero banners and wall-of-text.',verification:'verify_app runs the backend with your test inputs, renders this frontend in Chromium at 1280px and 390px with the real bridge, executes your scripted steps (click/fill/select/press by CSS selector), measures overflow and errors, and reviews the screenshots. Give interactive controls stable ids so tests can target them.'};
export async function fullDraftContract(){const {generationInstructions}=await import('./generation-instructions');return {...draftContract,instructions:generationInstructions};}
export function generationContract(kind:string){
 if(kind==='draft')return draftContract;
 if(kind==='wiki')return wikiRenderingContract;
 if(kind==='frontend')return frontendContract;
 if(kind==='program')return {fields:'program:{kind:"sandbox-program",language:"javascript"|"python",code:string}, inputFields:[{name,type:"string"|"number"|"boolean",description,required:boolean}], templateId:"files-v1"|"dashboard-v1"|"table-v1"|"form-v1"|"chat-v1"',instructions:programContract};
 if(kind==='chart')return {fields:'chart:{kind:"chart",xLabel,unit,series:[{key,label}],labels:{line,bar,data,download}}, dataset:{rows:[{x,values:{series_key:number|null},sources:[one_based_source_number]}],sources:[{title,url:https_url}],notes}, templateId:"dashboard-v1"|"table-v1"',rules:'Read actual observations through enabled connector/file tools or web search, as appropriate for the requested source. Do not web-search for private connector data. Preserve temporal operators exactly: since/from is an open-ended range through the latest available observation, while on is one date. Enumerate or paginate every source file in the requested range before drafting. At most 200 rows, 8 series; missing values null, not zero. All row values match series keys and units. Source every row. Never invent observations.'};
 if(kind==='form')return {fields:'form:{kind:"form",submit,fields:[{name,label,type:"number"|"text",default:null}],outputs:[{name,label}]},expression:{kind:"expression-program",outputs:{name:expression}},examples:[{input:{field:value},output:{name:value}}]',rules:'Expressions are a literal number/string, {input:"field"}, {step:"earlier_output"}, or {op,args:[expressions]}. Ops: add,multiply,min,max,concat (one or more args); subtract,divide,power (2); sqrt,abs,round,lowercase,uppercase,length,trim (1). Fields/output names match /^[a-z][a-z0-9_]{0,39}$/. Provide independently calculated examples. No JavaScript or external calls.'};
 if(kind==='converter')return {fields:'labels must include localized overview,value,from,to,convert,swap,result,working,choose,invalid,unavailable,length,mass,volume,time,temperature. Optional parameters:{value:number,from:unitId,to:unitId}',rules:'Registered units: mm,cm,m,km,in,ft,yd,mi,mg,g,kg,oz,lb,ml,l,us_gal,s,min,h,c,f,k. Supported categories: length,mass,volume,time,temperature. Use only compatible units. Other conversions require a form or program.'};
 if(kind==='custom_style')return customStyleFormat;
 throw Error('Unknown generation contract.');
}
