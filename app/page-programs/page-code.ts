import {z} from 'zod';
import {sandboxSchema} from '@/app/components-registry/sandbox-contracts';
// Page-scoped panels may be small toolbars or banners, unlike article illustrations.
export const pageCodeSchema=z.object({placement:z.enum(['before','after','replace']),frontend:sandboxSchema.extend({height:z.number().int().min(40).max(1200)})}).strict();
export type PageCode=z.infer<typeof pageCodeSchema>;
export const pageCodeContract={kind:'code',code:{placement:'before | after | replace',frontend:{kind:'sandbox-app',html:'HTML body',css:'CSS',javascript:'JavaScript',height:'40–1200'}},rules:'Supply executable frontend code, not instructions. before/after preserve the native app and add a custom panel. replace intentionally replaces its entire visible workspace, so reproduce all required existing functionality. Page files and chat remain. HTML/CSS/JavaScript run in an isolated iframe. On a page-program page, await window.pageTools.run(values,{query?,submitted?,message?,parent?,cursor?}) runs the page\u2019s own backend program and resolves {view,runtimeError,proposals}; keep logic in the backend and presentation here. Use await window.pageTools.call(name,args) for authorized workspace tools; it resolves the tool result (usually {data:...}). Discover connectors via list_connectors and resources via browse_resources({folderJson:"",cursor:""}); use returned schemas and real IDs. No credentials, direct network, parent DOM, imports or external scripts. Tool calls are unavailable in unsaved previews, but proposals are verified by running them for real (verify_app). Follow read_generation_contract kind=frontend for design quality. Do not perform mutations during rendering or loading; attach them to explicit user actions. Handle errors and empty/loading states. Use actual authorized data, never embedded private snapshots. Existing code is available via read_app_definition. Keep all unrelated native features unless the user requested replacing them. To delete a previously saved custom panel entirely and restore the native workspace, send {kind:"code",code:null}.'};
