'use client';
import {useEffect,useState} from 'react';import {FileBrowser,type FileEntry} from './file-browser';
export function UploadedFiles({language}:{language:string}){const [items,setItems]=useState<FileEntry[]>([]),[next,setNext]=useState<string|null>(null),[busy,setBusy]=useState(false),[error,setError]=useState('');const zh=language.startsWith('zh');async function load(after?:string){setBusy(true);setError('');try{const r=await fetch('/api/files'+(after?'?after='+encodeURIComponent(after):'')),d=await r.json() as {error?:string;items:FileEntry[];next:string|null};if(!r.ok)throw Error(d.error);setItems(old=>after?[...old,...d.items]:d.items);setNext(d.next);}catch(e){setError(String(e));}finally{setBusy(false);}}useEffect(()=>{void load();},[]);return <><div className="template-actions"><button disabled={busy} onClick={()=>void load()}>{zh?'刷新文件':'Refresh files'}</button>{busy&&<span role="status">{zh?'加载中…':'Loading…'}</span>}</div><FileBrowser items={items} title={zh?'我的上传':'My uploads'} language={language}/>{error&&<p role="alert">{error}</p>}{next&&<button disabled={busy} onClick={()=>void load(next)}>{zh?'加载更多':'Load more'}</button>}</>;}
