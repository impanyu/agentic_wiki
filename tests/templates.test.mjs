import test from 'node:test';import assert from 'node:assert/strict';import ts from 'typescript';import {readFileSync} from 'node:fs';
const {templates,searchTemplates,templateId}=await import('data:text/javascript;base64,'+Buffer.from(ts.transpile(readFileSync('app/templates/catalog.ts','utf8'),{module:ts.ModuleKind.ESNext,target:ts.ScriptTarget.ES2022})).toString('base64'));
test('catalog finds pre-coded layouts for different page purposes',()=>{for(const [q,id] of [['disambiguation meanings ambiguous','disambiguation-v1'],['wiki encyclopedia','wiki-v1'],['chat conversation','chat-v1'],['files folders directory hierarchy','files-v1'],['dashboard chart metrics','dashboard-v1'],['calculator converter','form-v1'],['table spreadsheet csv','table-v1']])assert.equal(searchTemplates(q)[0].id,id);assert.equal(new Set(templates.map(t=>t.id)).size,7);assert.equal(templateId('injected-code'),'wiki-v1');});
